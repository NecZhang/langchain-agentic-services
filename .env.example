# =============================================================================
# Enterprise Agentic Service Configuration
# =============================================================================

# =============================================================================
# vLLM Server Configuration
# =============================================================================
# Your vLLM server endpoint (required)
VLLM_ENDPOINT=http://192.168.6.10:8002

# Model to use (required)
VLLM_MODEL=Qwen/Qwen3-32B-FP8

# =============================================================================
# API Server Configuration
# =============================================================================
# Host to bind to (0.0.0.0 for all interfaces)
API_HOST=0.0.0.0

# Port to run the API server on
API_PORT=9510

# =============================================================================
# Language Configuration
# =============================================================================
# Default language (Chinese/English)
DEFAULT_LANGUAGE=Chinese

# Enable automatic language detection
AUTO_DETECT_LANGUAGE=true

# Supported languages (comma-separated)
SUPPORTED_LANGUAGES=Chinese,English

# =============================================================================
# Security Configuration
# =============================================================================
# API key for authentication (optional, set to enable)
API_KEY=your_secret_key_here

# Maximum file size in MB
MAX_FILE_SIZE_MB=50

# CORS allowed origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# =============================================================================
# Storage Configuration (IMPORTANT: Production vs Development)
# =============================================================================

# Option 1: External Data Directories (Recommended for Production)
# Set these to move data outside your project folder
AGENTIC_DATA_DIR=/var/lib/agentic-service
AGENTIC_TEMP_DIR=/tmp/agentic-uploads

# Option 2: Project Folders (Development Only - NOT for Production)
# Uncomment these for development (data stored in .data/ and .tmp_uploads/)
# AGENTIC_DATA_DIR=.data
# AGENTIC_TEMP_DIR=.tmp_uploads

# =============================================================================
# Advanced Storage (Future Implementation)
# =============================================================================
# Database for metadata (PostgreSQL/MySQL)
# DATABASE_URL=postgresql://user:password@localhost/agentic_db

# Object storage backend (S3/MinIO)
# STORAGE_BACKEND=s3
# S3_ENDPOINT=your-s3-endpoint
# S3_ACCESS_KEY=your-access-key
# S3_SECRET_KEY=your-secret-key
# S3_BUCKET=agentic-files

# Cache backend (Redis)
# CACHE_BACKEND=redis
# REDIS_URL=redis://localhost:6379

# =============================================================================
# Performance Configuration
# =============================================================================
# Maximum concurrent requests
MAX_CONCURRENT_REQUESTS=10

# Request timeout in seconds
REQUEST_TIMEOUT=300

# Chunking strategy (character, semantic, file_type, adaptive)
DEFAULT_CHUNKING_STRATEGY=adaptive

# Maximum chunks per document
MAX_CHUNKS_PER_DOC=1000

# =============================================================================
# Logging Configuration
# =============================================================================
# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Log file path (optional)
LOG_FILE=/var/log/agentic-service.log

# =============================================================================
# Network Proxy Configuration (for environments behind corporate firewall)
# =============================================================================
# Uncomment and configure if you need proxy to connect to external services
# HTTP_PROXY=http://proxy.company.com:8080
# HTTPS_PROXY=http://proxy.company.com:8080
# NO_PROXY=localhost,127.0.0.1,*.local,*.company.com

# Git/GitHub Proxy Configuration (if different from HTTP proxy)
# GIT_PROXY_COMMAND=proxy-command
# ALL_PROXY=socks5://proxy.company.com:1080

# =============================================================================
# Development Configuration
# =============================================================================
# Enable debug mode
DEBUG=false

# Enable auto-reload for development
AUTO_RELOAD=false

# Enable detailed error messages
DETAILED_ERRORS=false
STREAM_CHAR_BY_CHAR=true
FORCE_SMALL_CHUNKS=true
NETWORK_STREAMING_OPTIMIZED=true
DEBUG_STREAMING=false
